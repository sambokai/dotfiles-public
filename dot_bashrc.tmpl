# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac

# If zsh is available, immediately hand off to zsh
if command -v zsh >/dev/null 2>&1; then
    exec zsh
fi
